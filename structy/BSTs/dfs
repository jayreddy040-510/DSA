class Node:
    def __init__(self, val):
        self.val = val
        self.left = None
        self.right = None


def dfs(root):
    if root is None:
        return []
    left = dfs(root.left)
    right = dfs(root.right)
    return [root.val, *left, *right]


a = Node('a')
b = Node('b')
c = Node('c')
d = Node('d')
e = Node('e')
f = Node('f')

a.left = b
a.right = c
b.left = d
b.right = e
c.right = f

print(dfs(a))